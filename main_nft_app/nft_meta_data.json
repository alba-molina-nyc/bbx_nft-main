import json
from .models import Item
print(type(Item))

item_meta_data = json.loads(Item)
print(type(item_meta_data))
item_meta_data = {
   "model":"main_nft_app.item",
   "pk": self.pk,
   "fields":{
      "file": self.file,
      "name": self.name,
      "details_link": self.details_link,
      "description": self.description,
      "collection": self.collection,
      "properties": self.properties,
      "levels": self.levels,
      "stats": self.stats,
      "unlockable_content": self.unlockable_content,
      "explicit_or_sensitive_content": self.explicit_or_sensitive_content,
      "supply": self.supply,
      "blockchain": self.blockchain,
      "user": self.user
   }
}}
json.dump(item_meta_data, file)
print(json.dumps(item_meta_data))